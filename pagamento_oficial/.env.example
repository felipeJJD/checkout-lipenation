# ===== pagamento_oficial (frontend Vite/React) =====
# Copie para `.env` (dev) ou configure no build/plataforma.
# Aqui NÃO vão segredos — tudo que começa com VITE_ é embutido no bundle
# e fica visível no browser. Nunca colocar SK, senha ou token aqui.

# URL do backend (pagamento_oficial_v2). Embutida no bundle.
# Dev: http://localhost:3000
VITE_API_BASE_URL=https://exemplo-backend.up.railway.app
